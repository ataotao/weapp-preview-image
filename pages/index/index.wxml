<!-- index.wxml -->
<view class="container">
  <!-- 图片预览 -->
  <button class="btn" type="primary" bindtap="onTap">
    点击预览
  </button>
  <!-- <mp-gallery show="{{galleryShow}}" img-urls="{{galleryImgs}}" current="{{galleryCurrent}}"></mp-gallery> -->
  <preview-image show="{{galleryShow}}" img-urls="{{galleryImgs}}" current="{{galleryCurrent}}" />
</view>